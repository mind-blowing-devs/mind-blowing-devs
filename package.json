{
  "name": "client-server-template-with-vite",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && node init.js && lerna clean && yarn && lerna bootstrap",
    "preview:docker": "docker compose up client server redis postgres pgadmin",
    "build:docker": "docker compose build",
    "build": "lerna run build",
    "dev:client": "lerna run dev --scope=client",
    "dev:server": "docker compose up -d pgadmin postgres redis && lerna run dev --scope=server",
    "dev": "docker compose up -d pgadmin postgres redis && lerna run dev",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "preview": "lerna run preview",
    "prepare": "lefthook install"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@evilmartians/lefthook": "^1.3.9",
    "lerna": "^5.4.3"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0"
  },
  "packageManager": "yarn@1.22.22+sha512.a6b2f7906b721bba3d67d4aff083df04dad64c399707841b7acf00f6b133b7ac24255f2652fa22ae3534329dc6180534e98d17432037ff6fd140556e2bb3137e"
}
